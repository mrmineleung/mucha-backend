# from model.token import Token
#
#
# async def find_access_token(token: str):
#     return await Token.find_one(Token.access_token == token)
#
# async def find_refresh_token(token: str):
#     return await Token.find_one(Token.refresh_token == token)